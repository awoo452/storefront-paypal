default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: storefront_paypal_development

test:
  <<: *default
  database: storefront_paypal_test

production:
  primary: &primary
    url: <%= ENV["DATABASE_URL"] %>
  cable:
    <<: *primary
  queue:
    <<: *primary
  cache:
    <<: *primary
